import { lib } from "noname";
import { setConfig } from "../utils/hooks.js";

//更新说明
setConfig({
  introduce: {
    clear: true,
    name: `本扩展主要是对本体武将进行不同方向的强化设计或重构，设计来源于本人和广大网友。当然，除了对本体武将的重新设计外，本扩展还有一些奇奇怪怪的特殊武将。`,
    nopointer: true,
  }
});
